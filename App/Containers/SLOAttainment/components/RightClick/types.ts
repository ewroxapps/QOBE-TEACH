export interface ClickProps {
    marks?: Boolean ;
    setMarks: (value: boolean) => void;

    weights?: Boolean;
    setWeights: (value: boolean) => void;

    cqi?: Boolean;
    setCql: (value: boolean) => void;

    download?: Boolean;
    setDownload: (value: boolean) => void;

  }
  